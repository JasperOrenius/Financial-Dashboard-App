<UserControl x:Class="Financial_Dashboard_App.Views.NavigationBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Financial_Dashboard_App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200">
    <Grid Background="LightGray">
        <Button Content="🏠 Dashboard" HorizontalAlignment="Stretch" Margin="10,10,10,10" VerticalAlignment="Top" Height="45" FontSize="20" HorizontalContentAlignment="Left" Padding="10, -10" Command="{Binding Dashboard}"/>
        <Button Content="💰 Transactions" HorizontalAlignment="Stretch" Margin="10,60,10,0" VerticalAlignment="Top" Height="45" FontSize="20" HorizontalContentAlignment="Left" Padding="10, -10" Command="{Binding Transactions}"/>
        <Button Content="📁 Import Data" HorizontalAlignment="Stretch" Margin="10,110,10,0" VerticalAlignment="Top" Height="45" FontSize="20" HorizontalContentAlignment="Left" Padding="10, -10" Command="{Binding ImportData}"/>
        <Button Content="📊 Reports" HorizontalAlignment="Stretch" Margin="10,160,10,0" VerticalAlignment="Top" Height="45" FontSize="20" HorizontalContentAlignment="Left" Padding="10, -10" Command="{Binding Reports}"/>
    </Grid>
</UserControl>
